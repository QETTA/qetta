# QETTA 대시보드 설계 v2.0

## 설계 원칙

### 고객 여정 기반 설계

대시보드는 "기능 나열"이 아니라 **고객이 실제로 수행하는 작업 흐름**을 따라 구성합니다. 고객이 QETTA를 사용하는 이유는 "지원사업 신청 문서를 빨리 만들고 싶어서"입니다. 이 목표를 달성하기 위한 여정을 6단계로 정의합니다.

```
고객 여정 (Jobs-to-be-Done)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 발견 (Discover)     "어떤 지원사업이 나한테 맞지?"
   │
   ▼
2. 준비 (Prepare)      "우리 회사 정보로 BLOCK 설정해야지"
   │
   ▼
3. 생성 (Create)       "AI가 문서 만들어줘"
   │
   ▼
4. 검증 (Verify)       "제출 전에 검토해줘"
   │
   ▼
5. 제출 (Submit)       "제출하고 결과 추적하자"
   │
   ▼
6. 확장 (Expand)       "해외 입찰도 해볼까?"
```

### Linear 디자인 시스템 적용

QETTA는 Linear의 UI/UX 패턴을 적용합니다. 핵심 요소는 다음과 같습니다.

```typescript
const DESIGN_SYSTEM = {
  // 컬러
  colors: {
    background: {
      primary: "#0d0d0d",      // 메인 배경
      secondary: "#1a1a1a",    // 카드/패널 배경
      tertiary: "#262626"      // 호버 상태
    },
    accent: {
      primary: "#7C3AED",      // 보라색 (Primary)
      secondary: "#6366F1",    // 인디고 (Secondary)
      success: "#10B981",      // 초록 (성공)
      warning: "#F59E0B",      // 노랑 (경고)
      error: "#EF4444"         // 빨강 (에러)
    },
    text: {
      primary: "#FFFFFF",
      secondary: "#9CA3AF",
      tertiary: "#6B7280"
    }
  },
  
  // 상태 표시 아이콘
  statusIcons: {
    todo: "○",           // 빈 원 - 대기 중
    inProgress: "◐",     // 반 채움 - AI 처리 중
    done: "●",           // 채운 원 - 완료
    needsReview: "!",    // 검증 필요
    error: "✕"           // 에러
  },
  
  // 레이아웃
  layout: {
    sidebar: 240,        // px
    mainContent: "flex-1",
    rightPanel: 320      // px
  }
};
```

---

## 전체 대시보드 구조

### 라우팅 구조

```typescript
const DASHBOARD_ROUTES = {
  // 홈
  home: {
    path: "/dashboard",
    component: "HomePage"
  },
  
  // 발견 (Discover)
  discover: {
    path: "/dashboard/discover",
    children: {
      recommendations: "/discover/recommendations",
      calendar: "/discover/calendar",
      eligibility: "/discover/eligibility",
      alerts: "/discover/alerts"
    }
  },
  
  // BLOCK 관리
  blocks: {
    path: "/dashboard/blocks",
    children: {
      myBlocks: "/blocks/my-blocks",
      marketplace: "/blocks/marketplace",
      settings: "/blocks/settings",
      usage: "/blocks/usage"
    }
  },
  
  // 워크스페이스
  workspace: {
    path: "/dashboard/workspace",
    children: {
      projects: "/workspace/projects",
      documents: "/workspace/documents",
      editor: "/workspace/editor/:docId",
      templates: "/workspace/templates"
    }
  },
  
  // 검증
  verify: {
    path: "/dashboard/verify",
    children: {
      hashCheck: "/verify/hash",
      compliance: "/verify/compliance",
      aiReview: "/verify/ai-review",
      history: "/verify/history"
    }
  },
  
  // 제출
  submit: {
    path: "/dashboard/submit",
    children: {
      applications: "/submit/applications",
      tracker: "/submit/tracker",
      history: "/submit/history",
      analytics: "/submit/analytics"
    }
  },
  
  // EXPORT
  export: {
    path: "/dashboard/export",
    children: {
      opportunities: "/export/opportunities",
      studio: "/export/studio",
      translation: "/export/translation",
      credits: "/export/credits"
    }
  },
  
  // 분석
  analytics: {
    path: "/dashboard/analytics",
    children: {
      overview: "/analytics/overview",
      timeSaved: "/analytics/time-saved",
      successRate: "/analytics/success-rate",
      blockPerformance: "/analytics/blocks"
    }
  },
  
  // 설정
  settings: {
    path: "/dashboard/settings",
    children: {
      profile: "/settings/profile",
      team: "/settings/team",
      billing: "/settings/billing",
      integrations: "/settings/integrations",
      api: "/settings/api"
    }
  }
};
```

### 전체 레이아웃

```
┌─────────────────────────────────────────────────────────────────────┐
│ QETTA [BETA]  🔍 ⌘K                    [알림 🔔] [설정 ⚙️] [프로필] │
├──────────┬──────────────────────────────┬───────────────────────────┤
│          │                              │                           │
│ Sidebar  │       Main Content           │    Properties Panel       │
│ (240px)  │       (flex-1)               │        (320px)            │
│          │                              │                           │
│ ┌──────┐ │  ┌────────────────────────┐  │  ┌─────────────────────┐  │
│ │ 홈   │ │  │                        │  │  │  문서 정보          │  │
│ ├──────┤ │  │                        │  │  │  ─────────────────  │  │
│ │발견  │ │  │     [Tab Navigation]   │  │  │  상태: ● 완료       │  │
│ │      │ │  │                        │  │  │  카테고리: ENV     │  │
│ ├──────┤ │  │     [Content Area]     │  │  │  담당: @김민수     │  │
│ │BLOCKS│ │  │                        │  │  │  위임: @QETTA_AI   │  │
│ │      │ │  │                        │  │  │  해시: SHA-256 ✓   │  │
│ ├──────┤ │  │                        │  │  │                     │  │
│ │워크   │ │  │                        │  │  ├─────────────────────┤  │
│ │스페이스│ │  │                        │  │  │  Activity          │  │
│ ├──────┤ │  │                        │  │  │  ─────────────────  │  │
│ │검증  │ │  │                        │  │  │  10:30 AI 작업시작  │  │
│ │      │ │  │                        │  │  │  10:32 해시검증완료  │  │
│ ├──────┤ │  │                        │  │  │  10:35 문서생성완료  │  │
│ │제출  │ │  │                        │  │  │                     │  │
│ │      │ │  └────────────────────────┘  │  ├─────────────────────┤  │
│ ├──────┤ │                              │  │  Agent Actions      │  │
│ │EXPORT│ │                              │  │  ─────────────────  │  │
│ │      │ │                              │  │  [승인] [거부]      │  │
│ ├──────┤ │                              │  │  [위임] [수동처리]  │  │
│ │분석  │ │                              │  │                     │  │
│ └──────┘ │                              │  └─────────────────────┘  │
│          │                              │                           │
└──────────┴──────────────────────────────┴───────────────────────────┘
```

---

## 페이지별 상세 설계

### 1. 홈 페이지 (/dashboard)

홈 페이지는 **오버뷰와 빠른 액세스**를 제공합니다.

```
┌─────────────────────────────────────────────────────────────────────┐
│  안녕하세요, 김민수님 👋                            2026년 1월 30일  │
│  오늘 할 일 3건이 있습니다.                                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ⚡ Quick Actions                                            │   │
│  │  ─────────────────────────────────────────────────────────  │   │
│  │  [+ 새 문서 생성]  [🔍 지원사업 찾기]  [📊 리포트 보기]      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────┐  ┌─────────────────────┐                  │
│  │  📅 마감 임박        │  │  📊 이번 달 현황     │                  │
│  │  ─────────────────  │  │  ─────────────────  │                  │
│  │                     │  │                     │                  │
│  │  ! AI바우처 2차     │  │  문서 생성: 23건    │                  │
│  │    D-3              │  │  절약 시간: 47시간  │                  │
│  │                     │  │  성공률: 78%       │                  │
│  │  ○ 스마트공장       │  │                     │                  │
│  │    D-14             │  │  ████████░░ 23/50  │                  │
│  │                     │  │  이번 달 한도      │                  │
│  └─────────────────────┘  └─────────────────────┘                  │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  📝 최근 문서                                    [모두 보기]   │   │
│  │  ─────────────────────────────────────────────────────────  │   │
│  │                                                             │   │
│  │  ● QET-024  AI바우처 사업계획서      DIGITAL     오늘 10:30  │   │
│  │  ◐ QET-023  TMS 월간보고서          ENVIRONMENT  어제        │   │
│  │  ○ QET-022  스마트공장 신청서        MACHINERY    1/28       │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  💡 AI 추천                                                  │   │
│  │  ─────────────────────────────────────────────────────────  │   │
│  │                                                             │   │
│  │  📌 "SEMICONDUCTOR BLOCK을 추가하면 반도체 지원사업에       │   │
│  │      더 정확한 문서를 생성할 수 있습니다"                    │   │
│  │                                       [BLOCK 보기] [닫기]    │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

```typescript
interface HomePageComponents {
  // 인사 및 요약
  greeting: {
    userName: string;
    todayDate: Date;
    pendingTasks: number;
  };
  
  // 빠른 액션
  quickActions: [
    { label: "새 문서 생성", icon: "plus", action: "createDocument" },
    { label: "지원사업 찾기", icon: "search", action: "openDiscover" },
    { label: "리포트 보기", icon: "chart", action: "openAnalytics" }
  ];
  
  // 마감 임박 지원사업
  upcomingDeadlines: ProgramDeadline[];
  
  // 이번 달 현황
  monthlyStats: {
    documentsCreated: number;
    timeSavedHours: number;
    successRate: number;
    usageProgress: { used: number; limit: number };
  };
  
  // 최근 문서
  recentDocuments: RecentDocument[];
  
  // AI 추천
  aiSuggestions: AISuggestion[];
}
```

---

### 2. Discover 페이지 (/dashboard/discover)

고객이 **자신에게 맞는 지원사업을 찾는** 페이지입니다.

```
┌─────────────────────────────────────────────────────────────────────┐
│  지원사업 찾기                                                       │
│  구독 중인 BLOCK 기반으로 맞춤형 지원사업을 추천합니다.               │
├─────────────────────────────────────────────────────────────────────┤
│  [AI 추천] [전체 목록] [캘린더] [알림 설정]           [검색...]      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  🎯 내게 맞는 지원사업 (BLOCK 기반 추천)                             │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ⭐ 적합도 95%                                    마감 D-3    │   │
│  │  ─────────────────────────────────────────────────────────  │   │
│  │  AI 바우처 지원사업 (2026년 2차)                             │   │
│  │                                                             │   │
│  │  지원 규모: 최대 2억원 (정부 70%)                            │   │
│  │  대상: AI 솔루션 도입 희망 중소기업                          │   │
│  │  관련 BLOCK: DIGITAL, SEMICONDUCTOR                         │   │
│  │                                                             │   │
│  │  ✓ 귀사의 "AI 반도체 설계" 사업과 일치                      │   │
│  │  ✓ 과거 유사 신청 성공률 85%                                 │   │
│  │                                                             │   │
│  │  [자격 확인하기]  [문서 생성하기]                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ⭐ 적합도 88%                                   마감 D-14   │   │
│  │  ─────────────────────────────────────────────────────────  │   │
│  │  스마트제조혁신 지원사업 (스마트공장)                         │   │
│  │                                                             │   │
│  │  지원 규모: 최대 2.5억원 (정부 50%)                          │   │
│  │  대상: 중소·중견 제조기업                                    │   │
│  │  관련 BLOCK: SEMICONDUCTOR, MANUFACTURING                    │   │
│  │                                                             │   │
│  │  [자격 확인하기]  [문서 생성하기]                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ⭐ 적합도 75%                                   마감 D-30   │   │
│  │  ─────────────────────────────────────────────────────────  │   │
│  │  TIPS 프로그램 (기술창업)                                    │   │
│  │  ...                                                        │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

```typescript
interface DiscoverPageComponents {
  // 탭 네비게이션
  tabs: ["AI 추천", "전체 목록", "캘린더", "알림 설정"];
  
  // 추천 엔진
  recommendationEngine: {
    // 입력
    input: {
      companyProfile: CompanyProfile;
      subscribedBlocks: IndustryBlock[];
      pastApplications: Application[];
    };
    
    // 출력
    output: {
      recommendations: ProgramRecommendation[];
    };
  };
  
  // 추천 카드
  recommendationCard: {
    relevanceScore: number;           // 0-100
    daysUntilDeadline: number;
    programName: string;
    fundingAmount: string;
    targetCompanies: string;
    relatedBlocks: string[];
    matchReasons: string[];           // "귀사의 XX와 일치"
    estimatedSuccessRate: number;     // 과거 데이터 기반
    actions: ["자격 확인", "문서 생성"];
  };
  
  // 자격 확인 모달
  eligibilityChecker: {
    requirements: Requirement[];
    companyData: CompanyData;
    checkResult: {
      eligible: boolean;
      passedRequirements: string[];
      failedRequirements: string[];
      suggestions: string[];
    };
  };
}
```

---

### 3. BLOCKS 페이지 (/dashboard/blocks)

**BLOCK 관리**는 QETTA의 핵심 가치를 보여주는 페이지입니다.

```
┌─────────────────────────────────────────────────────────────────────┐
│  내 BLOCK                                                           │
│  산업별 전문 지식을 활용하여 더 정확한 문서를 생성하세요.             │
├─────────────────────────────────────────────────────────────────────┤
│  [내 BLOCK] [마켓플레이스] [사용량] [설정]                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  구독 중인 BLOCK (3/3)                               [PRO 플랜]     │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  ┌───────────────────┐ ┌───────────────────┐ ┌───────────────────┐ │
│  │                   │ │                   │ │                   │ │
│  │   💻 DIGITAL      │ │   🏭 SEMICONDUCTOR │ │   🌿 ENVIRONMENT  │ │
│  │   SW/AI/클라우드  │ │      반도체        │ │       환경        │ │
│  │                   │ │                   │ │                   │ │
│  │   ✓ 활성화       │ │   ✓ 활성화       │ │   ✓ 활성화       │ │
│  │                   │ │                   │ │                   │ │
│  │   📄 문서 45건   │ │   📄 문서 23건   │ │   📄 문서 12건   │ │
│  │   ⏱️ 89시간 절약 │ │   ⏱️ 42시간 절약 │ │   ⏱️ 28시간 절약 │ │
│  │   📈 정확도 94%  │ │   📈 정확도 91%  │ │   📈 정확도 96%  │ │
│  │                   │ │                   │ │                   │ │
│  │   [설정] [통계]  │ │   [설정] [통계]  │ │   [설정] [통계]  │ │
│  └───────────────────┘ └───────────────────┘ └───────────────────┘ │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  💡 BLOCK 추가 추천                                                 │
│                                                                     │
│  "귀사의 사업 영역을 분석한 결과, AUTOMOTIVE BLOCK을 추가하면       │
│   자율주행 관련 지원사업 문서의 정확도가 높아질 것으로 예상됩니다"   │
│                                                                     │
│   TEAM 플랜으로 업그레이드하면 전체 12개 BLOCK을 사용할 수 있습니다. │
│                                              [TEAM 업그레이드]      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

```typescript
interface BlocksPageComponents {
  // 내 BLOCK 카드
  myBlockCard: {
    id: string;
    icon: string;
    name: string;
    nameKo: string;
    status: "active" | "inactive";
    stats: {
      documentsGenerated: number;
      timeSavedHours: number;
      accuracyRate: number;
    };
    actions: ["settings", "stats"];
  };
  
  // BLOCK 설정 모달
  blockSettings: {
    blockId: string;
    preferences: {
      defaultTemplate: string;
      writingStyle: "formal" | "semi-formal";
      autoSuggestions: boolean;
    };
    customTerms: Map<string, string>;  // 회사 고유 용어
  };
  
  // BLOCK 통계
  blockStats: {
    usage: {
      daily: number[];
      weekly: number[];
      monthly: number[];
    };
    accuracy: {
      overall: number;
      byDocumentType: Map<string, number>;
    };
    timeSaved: {
      total: number;
      average: number;
    };
  };
  
  // 업그레이드 유도
  upgradePrompt: {
    currentTier: string;
    currentBlockLimit: number;
    recommendedBlocks: string[];
    upgradeMessage: string;
  };
}
```

---

### 4. Workspace 페이지 (/dashboard/workspace)

**문서 생성의 핵심** 페이지입니다. Linear의 이슈 관리 패턴을 적용합니다.

```
┌─────────────────────────────────────────────────────────────────────┐
│  워크스페이스                                                        │
├─────────────────────────────────────────────────────────────────────┤
│  [모든 문서] [진행 중] [완료]  [Filter ▼] [Display ▼]  [+ 새 문서]  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ○ TRIAGE (3)                                                │   │
│  │  ─────────────────────────────────────────────────────────  │   │
│  │  신규 요청 - AI가 분류를 추천합니다                          │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │  ◐ QET-027  스마트공장 사업계획서 초안 작성 요청             │   │
│  │             💡 MACHINERY BLOCK 추천                         │   │
│  │  ◐ QET-026  환경영향평가 보고서 업데이트                     │   │
│  │             💡 ENVIRONMENT BLOCK 추천                       │   │
│  │  ◐ QET-025  AI바우처 기술제안서                              │   │
│  │             💡 DIGITAL BLOCK 추천                           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ◐ IN PROGRESS (2)                                          │   │
│  │  ─────────────────────────────────────────────────────────  │   │
│  │  QETTA AI가 작업 중입니다                                    │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │  ◐ QET-024  AI바우처 사업계획서                              │   │
│  │             🏷️ DIGITAL        @QETTA_AI 처리 중 (75%)        │   │
│  │  ◐ QET-023  TMS 월간보고서                                   │   │
│  │             🏷️ ENVIRONMENT    @QETTA_AI 검토 대기            │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  ● DONE (15)                                     [모두 보기]   │   │
│  │  ─────────────────────────────────────────────────────────  │   │
│  │  완료된 문서                                                 │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │  ● QET-022  스마트공장 신청서     🏷️ MACHINERY     1/28     │   │
│  │  ● QET-021  TIPS 사업계획서       🏷️ DIGITAL       1/27     │   │
│  │  ● QET-020  환경오염방지 보고서   🏷️ ENVIRONMENT   1/25     │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

```typescript
interface WorkspacePageComponents {
  // 탭 네비게이션
  tabs: ["모든 문서", "진행 중", "완료"];
  
  // 필터
  filters: {
    block: IndustryBlock[];
    status: DocumentStatus[];
    dateRange: DateRange;
    assignee: User[];
  };
  
  // 디스플레이 옵션
  displayOptions: {
    view: "list" | "board" | "table";
    groupBy: "status" | "block" | "project" | "none";
    sortBy: "date" | "priority" | "status";
  };
  
  // 문서 리스트 아이템
  documentListItem: {
    id: string;                         // "QET-024"
    title: string;
    status: "triage" | "in_progress" | "done" | "needs_review";
    statusIcon: "○" | "◐" | "●" | "!";
    block: IndustryBlock;
    assignee: User | null;
    delegate: "QETTA_AI" | null;
    delegateProgress: number | null;    // 0-100
    updatedAt: Date;
    aiSuggestion: string | null;        // TRIAGE 상태일 때
  };
  
  // Triage Intelligence (Linear 패턴)
  triageIntelligence: {
    // 새 문서가 들어오면 AI가 분석
    autoAnalysis: {
      suggestedBlock: IndustryBlock;
      suggestedTemplate: DocumentTemplate;
      duplicateCheck: DuplicateResult;
      prioritySuggestion: Priority;
    };
    
    // 자동화 옵션
    automation: {
      autoApplyBlock: boolean;
      autoApplyTemplate: boolean;
      autoAssign: boolean;
    };
  };
}
```

---

### 5. 문서 에디터 (/dashboard/workspace/editor/:docId)

AI와 협업하여 문서를 작성하는 핵심 화면입니다.

```
┌─────────────────────────────────────────────────────────────────────┐
│  ← QET-024 AI바우처 사업계획서                      [저장] [내보내기]│
├──────────────────────────────────────┬──────────────────────────────┤
│                                      │                              │
│  📄 문서 편집                        │  🤖 QETTA AI                 │
│  ────────────────────────────────    │  ──────────────────────────  │
│                                      │                              │
│  ┌──────────────────────────────┐    │  ┌────────────────────────┐  │
│  │ 1. 사업 개요                 │    │  │ 현재 작업:             │  │
│  │                              │    │  │ "1. 사업 개요" 섹션    │  │
│  │ (주)ABC는 AI 반도체 설계    │    │  │ 작성 중...             │  │
│  │ 전문 기업으로서...          │    │  │                        │  │
│  │                              │    │  │ ████████░░ 75%        │  │
│  │ [AI가 작성 중...]           │    │  └────────────────────────┘  │
│  │                              │    │                              │
│  ├──────────────────────────────┤    │  💬 대화                     │
│  │ 2. 기술 현황                 │    │  ──────────────────────────  │
│  │                              │    │                              │
│  │ ○ 아직 작성되지 않음        │    │  나: "사업 개요에 우리      │
│  │                              │    │       회사의 수상 이력도    │
│  ├──────────────────────────────┤    │       포함해줘"             │
│  │ 3. 사업 목표                 │    │                              │
│  │                              │    │  AI: "네, 귀사의 수상       │
│  │ ○ 아직 작성되지 않음        │    │       이력을 추가했습니다:  │
│  │                              │    │       - 2025 혁신기업 대상  │
│  ├──────────────────────────────┤    │       - CES 2026 혁신상"    │
│  │ 4. 추진 일정                 │    │                              │
│  │                              │    │  ──────────────────────────  │
│  │ ○ 아직 작성되지 않음        │    │                              │
│  │                              │    │  [메시지 입력...]     [전송] │
│  └──────────────────────────────┘    │                              │
│                                      │                              │
│  ──────────────────────────────────  │  💡 AI 제안                  │
│  🏷️ DIGITAL  📊 진행률 25%          │  ──────────────────────────  │
│  ⏱️ 예상 완료: 10분                  │  "기술 현황" 섹션에         │
│                                      │  특허 목록을 추가하면       │
│                                      │  가점을 받을 수 있습니다.   │
│                                      │                   [적용하기] │
│                                      │                              │
└──────────────────────────────────────┴──────────────────────────────┘
```

```typescript
interface DocumentEditorComponents {
  // 문서 메타데이터
  documentMeta: {
    id: string;
    title: string;
    block: IndustryBlock;
    template: DocumentTemplate;
    status: DocumentStatus;
    progress: number;
    estimatedCompletion: number;  // 분
  };
  
  // 문서 섹션
  sections: DocumentSection[];
  
  // AI 협업 패널
  aiPanel: {
    // 현재 작업 상태
    currentTask: {
      section: string;
      status: "writing" | "reviewing" | "waiting";
      progress: number;
    };
    
    // 대화 히스토리
    conversation: Message[];
    
    // AI 제안
    suggestions: AISuggestion[];
  };
  
  // Agent 위임 (Linear 패턴)
  agentDelegation: {
    delegate: "QETTA_AI";
    assignee: User;          // 최종 책임자
    delegatedSections: string[];
    humanReviewRequired: boolean;
  };
}

interface DocumentSection {
  id: string;
  title: string;
  content: string;
  status: "draft" | "ai_writing" | "ai_complete" | "human_edited" | "approved";
  aiConfidence: number;      // 0-100
  lastEditedBy: "human" | "ai";
  lastEditedAt: Date;
}
```

---

### 6. EXPORT 페이지 (/dashboard/export)

**글로벌 입찰 문서** 전용 프리미엄 페이지입니다.

```
┌─────────────────────────────────────────────────────────────────────┐
│  EXPORT                                                             │
│  글로벌 입찰에 필요한 영문 문서를 생성하세요.                        │
├─────────────────────────────────────────────────────────────────────┤
│  [기회 탐색] [문서 작업] [번역] [크레딧]                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  💳 내 크레딧: 45 크레딧                              [크레딧 충전]  │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  🌐 추천 글로벌 기회                                                │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  🇺🇸 SAM.gov                                    마감 D-21    │   │
│  │  ─────────────────────────────────────────────────────────  │   │
│  │  AI Semiconductor Development for Defense                   │   │
│  │                                                             │   │
│  │  예상 규모: $2.5M                                           │   │
│  │  관련 BLOCK: SEMICONDUCTOR, DIGITAL                         │   │
│  │  필요 크레딧: 15 (풀 패키지)                                 │   │
│  │                                                             │   │
│  │  [상세 보기]  [제안서 생성 시작]                              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  🇺🇳 UNGM                                       마감 D-45    │   │
│  │  ─────────────────────────────────────────────────────────  │   │
│  │  Environmental Monitoring System                            │   │
│  │                                                             │   │
│  │  예상 규모: $500K                                           │   │
│  │  관련 BLOCK: ENVIRONMENT                                     │   │
│  │  필요 크레딧: 10 (풀 패키지)                                 │   │
│  │                                                             │   │
│  │  [상세 보기]  [제안서 생성 시작]                              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 공통 컴포넌트

### 사이드바 네비게이션

```typescript
interface SidebarNavigation {
  sections: [
    {
      title: null,  // 제목 없음
      items: [
        { icon: "home", label: "홈", path: "/dashboard" }
      ]
    },
    {
      title: "작업",
      collapsible: true,
      items: [
        { icon: "search", label: "발견", path: "/dashboard/discover" },
        { icon: "blocks", label: "BLOCKS", path: "/dashboard/blocks", badge: "3" },
        { icon: "folder", label: "워크스페이스", path: "/dashboard/workspace" },
        { icon: "check", label: "검증", path: "/dashboard/verify" },
        { icon: "send", label: "제출", path: "/dashboard/submit" }
      ]
    },
    {
      title: "프리미엄",
      collapsible: true,
      items: [
        { icon: "globe", label: "EXPORT", path: "/dashboard/export", badge: "45 크레딧" }
      ]
    },
    {
      title: "인사이트",
      collapsible: true,
      items: [
        { icon: "chart", label: "분석", path: "/dashboard/analytics" }
      ]
    }
  ];
  
  footer: {
    items: [
      { icon: "settings", label: "설정", path: "/dashboard/settings" },
      { icon: "help", label: "도움말", path: "/help" }
    ]
  };
}
```

### Properties Panel (우측 패널)

Linear 패턴의 속성 패널입니다.

```typescript
interface PropertiesPanel {
  // 문서 정보
  documentInfo: {
    status: DocumentStatus;
    category: IndustryBlock;
    verificationDate: Date | null;
    hashValue: string | null;
    assignee: User;
    delegate: Agent | null;
  };
  
  // Activity 피드
  activityFeed: Activity[];
  
  // Agent Actions
  agentActions: {
    approve: () => void;
    reject: () => void;
    delegate: () => void;
    manualProcess: () => void;
  };
}

interface Activity {
  id: string;
  type: "creation" | "delegation" | "verification" | "completion" | "comment";
  message: string;
  actor: User | Agent;
  timestamp: Date;
}
```

### AI Suggestions Widget

```typescript
interface AISuggestionsWidget {
  suggestions: AISuggestion[];
  
  // 각 제안
  suggestion: {
    type: "duplicate_detected" | "label_recommendation" | "block_recommendation" | "quality_warning";
    message: string;
    confidence: number;
    actions: Action[];
  };
  
  // 자동화 설정
  automationSettings: {
    showSuggestions: boolean;
    autoApply: boolean;
    confidenceThreshold: number;  // 이 이상이면 자동 적용
  };
}
```
